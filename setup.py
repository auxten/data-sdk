from setuptools import setup, find_packages

setup(
    name="data-sdk",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here
    ],
    python_requires=">=3.7",
)
